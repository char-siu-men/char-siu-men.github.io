machine:
  environment:
    USER_NAME: a5-receptionist
    USER_EMAIL: receptionist@altus5.com
  node:
    version: 4.7.1
  timezone:
    Asia/Tokyo

dependencies:
  - npm install
  - npm run build

deployment:
  production:
    branch: draft
    commands:
      - sh ./bin/automated
